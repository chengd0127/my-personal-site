# Guodong Chen CV Website

Lightweight static CV website built with plain HTML, CSS, and JavaScript modules.

## Commands

```bash
npm run build
npm run start
```

The build output is written to `dist/`.

## Update Profile Data

Edit `data/profile.json` to update personal details, contact links, education, experience, projects, and skills.
